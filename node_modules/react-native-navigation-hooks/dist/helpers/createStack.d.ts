import { Options, LayoutStackChildren, LayoutStack } from 'react-native-navigation';
/**
 * Creates navigation LayoutStack exposing multiple function types for the same function as a list of overloads.
 * [more info](https://wix.github.io/react-native-navigation/api/layout-stack)
 */
declare function createStack(layoutStackChildren: LayoutStackChildren, id?: string, options?: Options): LayoutStack;
declare function createStack(layoutStackChildrenArray: LayoutStackChildren[], id?: string, options?: Options): LayoutStack;
declare function createStack(name: string): LayoutStack;
declare function createStack<P = {}>(name: string, passProps?: P): LayoutStack;
declare function createStack<P = {}>(name: string, passProps?: P, options?: Options): LayoutStack;
export default createStack;
//# sourceMappingURL=createStack.d.ts.map